#include <bits/stdc++.h>
#define ll long long
using namespace std;

int main() {
    ll H,W;
    int N;
    cin >> H >> W >> N;
    vector<vector<ll>> Card(H,vector<ll>(W,0));
    for(int i = 0;i < N;i++){
        ll A,B;
        cin >> A >> B;
    }
    return 0;
}